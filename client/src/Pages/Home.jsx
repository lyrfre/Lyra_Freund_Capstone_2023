import React from "react";
import Login from "../components/Login";

const Home = ({ user }) => {
  // if (!user) return <Login />;

  return (
    <div>
      <h1>Home</h1>
    </div>
  );
};

export default Home;
